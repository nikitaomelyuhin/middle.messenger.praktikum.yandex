export { SidebarList as default } from "./chatList";

// import { SidebarList } from "./chatList";
// import { withStore } from "../../utils/Store";

// const withChat = withStore((state) => ({ ...state.sidebarData }));

// export default withChat(SidebarList);
