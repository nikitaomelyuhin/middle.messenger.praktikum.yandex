<button class="app-button {{type}} {{additionalType}}">
  {{#if link}}
    <a class="app-button__link" href="{{link}}">{{text}}</a>
    {{else}}
    <p class="app-button__text">{{text}}</p>
  {{/if}}
</button>